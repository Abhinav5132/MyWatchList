Backend:
    -add tag percentages
    - use anyhow fo errors 
    -logging out on account page should redirect back to main menu
    - if a value for something is unknown skip it. 
Frontend:
    -Clicking on a friend liking it should also take them to a different page with when they added ect and their other likes/watches
    -trash can image is not the right image.
    -plus icon needs to be replaced.
    -add a showing results for on search page.
    -fix the load_times for images somehow // need to switch to serving smaller images 
    -recommend button is only reactive if hovering over the image
    -popup only occurs when u click the image not the text 
    -removing and adding username from the local store 
    - on first install check initialization and if that process has started show a progrress bar or a spinny animation thingy.
    - fix task bar css by scaling the components instead of moving the things beside it
    -refresh pages when stuff happens 
    -adding images for lists is broken 
    -search bar white on white test 
    -css for the login page 
    - strip spaces when the user enters username, passwoers search queries ect

prod:
    -https certificate
    -the install script should check to see if they ahve a wallet manager if not install one
    
future-implementations: 
    -file linking with anime and episodes, 
    -watch tracking on those files
    -make a browser extention to track watching from other sources
    -


-- not to self: Signal sitrings dont need to be dereferenced