services:
  database:
      image: postgres:latest
      environment:
        POSTGRES_DB: MyWatchListDb
        POSTGRES_USER: Admin
        POSTGRES_PASSWORD: Test1314
      ports:
        - "5432:5432"
      volumes:
        - db-data:/var/lib/postgresql
      networks:
        - app-network

networks:
  app-network:

volumes:
  db-data: